---
import Earth from "./Earth.svelte";

const locale = Astro.locals.locale;

const strings = {
  en: {
    roles: { developer: "Software developer", nomad: "Visual nomad" },
    bio: {
      build: "I build software for a living,",
      design: "design for the human experience,",
      capture: "and capture the world through a lens",
      remember: "to remember why it all matters.",
    },
    location: "Based in Slovakia",
    building: "Currently building stuff at",
    findMe: "Find me on",
    say: "Say",
  },
  sk: {
    roles: { developer: "Softvérový vývojár", nomad: "Vizuálny nomád" },
    bio: {
      build: "Živím sa tvorbou softvéru,",
      design: "dizajnujem pre ľudský zážitok,",
      capture: "zachytávam svet cez objektív,",
      remember: "aby som nezabudol, prečo na tom záleží.",
    },
    location: "Žijem na Slovensku",
    building: "Momentálne tvorím vo",
    findMe: "Nájdeš ma na",
    say: "Napíš na",
  },
};
const t = strings[locale as keyof typeof strings];
---

<div class="home">
  <h1>
    <span>{t.roles.developer}</span>
    <span>·</span>
    <span>{t.roles.nomad}</span>
  </h1>

  <div class="bio">
    <div>{t.bio.build}</div>
    <div>{t.bio.design}</div>
    <div>{t.bio.capture}</div>
    <div>{t.bio.remember}</div>
  </div>

  <div class="details">
    <div>{t.location}</div>
    <div>
      {t.building}
      <a href="https://visma.com">Visma Labs</a>
    </div>
    <div>
      {t.findMe}
      <a href="https://instagram.com/ja.vlado">Instagram</a> · <a
        href="https://github.com/lavyda">Github</a
      >
    </div>
    <span>{t.say} <a href="mailto:hi@kovalsky.sk">hi@kovalsky.sk</a></span>
  </div>

  <div class="backdrop" aria-hidden="true">
    <Earth client:idle color="var(--color-accent)" />
  </div>
</div>

<style>
  .home {
    isolation: isolate;
    padding-block-start: var(--space-3xl);
  }

  h1 {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    font-family: var(--font-monospace);
    font-size: var(--text-m);
    font-weight: var(--weight-normal);
    text-transform: uppercase;
    color: var(--color-accent);
  }

  .bio {
    margin-block: var(--space-xl);
    font-size: var(--text-3xl);

    & > div:nth-child(2) {
      opacity: 0.85;
    }

    & > div:nth-child(3) {
      opacity: 0.75;
    }

    & > div:nth-child(4) {
      opacity: 0.65;
    }
  }

  .details {
    inline-size: min(50ch, 100%);
    display: grid;
    gap: var(--space-xs);
    color: var(--color-text-muted);
    font-family: var(--font-monospace);
    border-top: 1px solid var(--color-border);
    padding-block-start: var(--space-m);
  }

  .backdrop {
    position: fixed;
    z-index: -1;
    right: clamp(-8rem, -6vw, -3rem);
    bottom: clamp(-8rem, -6vw, -3rem);
    inline-size: clamp(22rem, 55vw, 48rem);
    aspect-ratio: 1;
    pointer-events: none;
  }
</style>
