---
import { getRelativeLocaleUrl } from "astro:i18n";

const { locale } = Astro.locals;

const strings = {
  en: {
    blog: "Blog",
    gallery: "Gallery",
  },
  sk: {
    blog: "Blog",
    gallery: "Galéria",
  },
};
const t = strings[locale as keyof typeof strings];
---

<nav>
  <ul>
    <li><a href={getRelativeLocaleUrl(locale, "blog")}>{t.blog}</a></li>
    <li>
      <a href={getRelativeLocaleUrl(locale, "gallery")}>{t.gallery}</a>
    </li>
  </ul>
</nav>

<style>
  nav {
    ul {
      display: flex;
      flex-direction: row;
      list-style-type: none;
      gap: var(--space-l);

      a {
        text-decoration: none;

        &:hover,
        &:active,
        &:focus-visible {
          text-decoration: underline;
        }
      }
    }
  }
</style>
