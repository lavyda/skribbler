---
import { ClientRouter } from "astro:transitions";
import "@/styles/reset.css";
import "@/styles/main.css";
import Header from "@/modules/core/Header.astro";

interface Props {
	title?: string;
}

const { title = "Vladimír Kovalský" } = Astro.props;
const { locale } = Astro.locals;
---

<!doctype html>
<html lang={locale}>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="color-scheme" content="light dark" />
		<script is:inline>
			function applyTheme() {
				const t = localStorage.getItem("theme");
				if (t === "light" || t === "dark")
					document.documentElement.dataset.theme = t;
				else document.documentElement.removeAttribute("data-theme");
			}
			applyTheme();
			document.addEventListener("astro:after-swap", applyTheme);
		</script>
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<ClientRouter />
		<script>
			import { initCoverTransitions } from "@/lib/transitions/coverTransition";
			initCoverTransitions();
		</script>

		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Mozilla+Text:wght@200..700&display=swap"
			rel="stylesheet"
		/>
		<link
			href="https://fonts.googleapis.com/css2?family=Saira:ital,wght@0,100..900;1,100..900&display=swap"
			rel="stylesheet"
		/>
		<link
			href="https://fonts.googleapis.com/css2?family=Source+Code+Pro:ital,wght@0,200..900;1,200..900&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body>
		<Header />
		<slot />
	</body>
</html>

<style>
	body {
		display: grid;
		grid-template-rows: auto 1fr;
	}
</style>
