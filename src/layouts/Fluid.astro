---
import Base from "./Base.astro";

interface Props {
	title?: string;
}

const { title } = Astro.props;
---

<Base {title}>
	<main>
		<slot />
	</main>
</Base>

<style>
	main {
		padding: var(--space-l) var(--space-3xl);
	}
</style>
